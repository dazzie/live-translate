fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
aiosqlite==0.20.0

# --- LLM providers (this build supports anthropic + openrouter, see lib/llm.py) ---
anthropic==0.39.0
openai==1.51.0        # used for LLM_PROVIDER=openrouter (OpenAI-compatible API)
httpx==0.27.2         # anthropic 0.39.0 is incompatible with httpx>=0.28 (dropped `proxies`)
# google-genai==0.8.0
